{
  "name": "som-schedule",
  "description": "A recommended schedule for Schools of Ministry.",
  "version": "1.0.0",
  "scripts": {
    "build": "node ./build.js"
  },
  "dependencies": {
    "ejs": "^3.1.3",
    "marked": "^1.1.0"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "*.{js,css,json,md,ts,tsx}": [
      "prettier --write"
    ]
  },
  "prettier": "@dmsi/prettier-config",
  "devDependencies": {
    "@dmsi/prettier-config": "^1.3.0",
    "husky": "^4.2.5",
    "lint-staged": "^10.2.2",
    "prettier": "^2.0.5"
  }
}
